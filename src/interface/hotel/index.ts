import { iAddress } from "@interface/address";

export interface iHotel {
  id: string;
  name: string;
  email: string;
  num_rooms: number;
  address: iAddress;
  full_url: string;
  full_url2: string;
  full_url3: string;
  full_url4: string;
  full_url5: string;
}
